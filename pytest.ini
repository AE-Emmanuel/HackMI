[pytest]
markers =
    live: integration tests that call watsonx.ai (slow, costs tokens)
testpaths = tests
# Skip live tests by default; opt in with `pytest -m live`.
addopts = -ra -q --strict-markers -m "not live"
